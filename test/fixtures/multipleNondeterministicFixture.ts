export const randomNumberOne = (min: number, max: number): number =>
  Math.floor(Math.random() * (max - min + 1) + min);

export const randomNumberTwo = (min: number, max: number): number =>
  Math.floor(Math.random() * (max - min + 1) + min);
